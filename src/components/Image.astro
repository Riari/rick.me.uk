---
const { src, alt, caption, maxWidth = "auto" } = Astro.props;
---

<div class="image-container">
    <img src={src} alt={alt} style={{ maxWidth }} data-zoomable />
    { caption && <div class="caption">{caption}</div> }
</div>